# AI Retail Analytics — frontend environment configuration.
# Copy this file to .env.local and adjust the backend URL if needed.
# NEXT_PUBLIC_* values are visible in the browser; never put secrets here.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
