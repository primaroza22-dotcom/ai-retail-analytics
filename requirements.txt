# AI Retail Analytics Platform - runtime dependencies
#
# Sprint 2: camera input (RTSP frame capture).
opencv-python>=4.8
numpy>=1.24

# Sprint 3: YOLO person detection.
ultralytics>=8.0

# Sprint 4: object tracking (ByteTrack backend).
boxmot>=22.0

# Sprint 6: FastAPI + PostgreSQL (API layer and persistence).
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
psycopg[binary]>=3.1
pydantic-settings>=2.2

# Sprint 9: database migrations.
alembic>=1.13

# Sprint 13/14: IANA timezone database for business-timezone analytics
# (required on Windows; provided by the OS on most Linux distributions).
tzdata>=2024.1

# Test dependencies are declared in pyproject.toml.
