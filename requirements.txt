# AI Retail Analytics Platform - runtime dependencies
#
# Sprint 2: camera input (RTSP frame capture).
opencv-python>=4.8
numpy>=1.24

# AI / vision dependencies (e.g. ultralytics) will be added in later sprints.
# Test dependencies are declared in pyproject.toml.
